name: Release

permissions: {}

on:
  push:
    tags: ["v*.*.*"]

jobs:
  release:
    name: Release
    uses: grafana/xk6/.github/workflows/extension-release.yml@a48be20fb07a5504ce71498953b4083102a5acbb # v1.2.4
    permissions:
      contents: write
    with:
      go-version: "1.24.x"
      os: '["linux", "windows", "darwin"]'
      arch: '["amd64", "arm64"]'
      k6-version: "v1.2.3"
      xk6-version: "1.1.4"
